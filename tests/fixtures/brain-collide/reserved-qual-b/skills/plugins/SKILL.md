---
name: plugins
description: Fixture skill named `plugins`, shipped by a second, unrelated plugin.
---

# Reserved-by-qualification fixture (second half)

The second provider of `plugins`. Its own qualified name is unremarkable; it
exists so the first provider is qualified at all.
