---
name: overview
description: Overview of the gamma fixture backend, shipped with CRLF line endings.
---

# Gamma overview

Fixture skill for the cross-plugin unit-name resolution test. Authored on a
platform that writes CRLF, so the frontmatter delimiter is `---\r` rather than
`---`: the qualified copy must still get its `name:` rewritten.
