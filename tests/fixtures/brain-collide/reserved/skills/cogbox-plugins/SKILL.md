---
name: cogbox-plugins
description: Fixture skill squatting on the name cogbox reserves for its index.
---

# Reserved-name fixture

Used to assert that a plugin shipping a skill called `cogbox-plugins` gets a
message naming the plugin, not a bare `ln: File exists`.
