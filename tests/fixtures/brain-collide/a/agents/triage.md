---
name: triage
description: Alpha fixture triage agent.
---

Fixture agent for the cross-plugin unit-name resolution test. An agent is a
single `.md` file, which is the other half of the qualified-copy code path.

The line below starts at column 0 and must survive the frontmatter rewrite
untouched: only the leading `---` block is metadata.

name: not-metadata
